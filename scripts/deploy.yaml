apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: flask
  name: python-flask-app-deployment
spec:
  selector:
    matchLabels:
      app: flask
  template:
    metadata:
      labels:
        app: flask
    spec:
      containers:
      - image: manojjahgirdar/ddc-argocd-demo:v9
        name: ddc-argocd-demo
